// 1. variable
var myName = "khan";

// math operation: +, -, *, /, %
// shorthand: +=, -=, *=, /=
// increase, decrease: ++, --

// 2. array
var friends = ["abul", "babul", "cabul", "dabul", "ebul"];

var thirdFriend = friends[2];
friends[3] = "dambool";

// 3. conditionals
// <, >, <=, >=, ==, !=
if (friends.length < 2) {
    // statement
}
else {
    // statement
};

// 4. loop
var number = 0;
while (number <= 6) {
    console.log();
    number++;
};

for (var i = 0; i <= 6; i++) {
    // statement
};

// 5. function
function isMoonUp(time) {
    if (time > 7) {
        return true;
    }

};

// 6. object
var jantus = {
    height: 60,
    romanticism: "heavy",
    baperTaka: "borolox",
};